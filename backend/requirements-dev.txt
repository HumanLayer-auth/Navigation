# 분석 노트북 전용 의존성. 서버 실행에는 필요 없다 — Docker 이미지에도 넣지 않는다.
#   backend/notebooks/faiss_eval.ipynb  (FAISS 임계값 평가)
#   backend/notebooks/kiwi_eval.ipynb   (Kiwi 형태소 정규화 전수 평가)
#
# 설치: .venv 활성화 후
#   python -m pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pandas>=2.0
matplotlib>=3.8
jupyter>=1.0
nbconvert>=7.0
ipykernel>=6.0
